<div  style="text-align: center;">
    <h1>Biblia Sagrada</h1>

<h2>Todos os livros</h2>
<table class="table table-striped">
    <thead>
        <tr>
            <th scope="col">Livros</th>
        </tr>
    </thead>
    <tbody>
        
            {{#each livros}}
            <form action="/biblia/livro" method="post">
            <input type="hidden" name="name" value="{{this}}">
            <tr>
                <td>
                    <input style="background-color: rgba(0, 0, 0, 0);color: black;border:none;" class="btn btn-secondary btn-lg btn-block" type="submit" value="{{this}}">
                </td>
            </tr>
</form>
            {{else}}

            {{/each}}
        
    </tbody>
</table>
</div>
